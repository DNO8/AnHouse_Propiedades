---
const items = [
  {
    title: "Activos",
    content:
      "Son bienes de diferente naturaleza (propiedades o bienes raíces, depósitos a plazo, cuotas de fondos mutuos, derechos, acciones de sociedades anónimas, vehículos, participación en sociedades, cuentas y/o libretas de ahorro a plazo e inversiones de otra naturaleza) que posee una empresa o persona y que tienen un valor económico."
  },
  {
    title: "Amortización",
    content:
      "Es el pago parcial o total del capital de un préstamo. Se entiende además como el componente del dividendo a pagar por un préstamo hipotecario, que no corresponde a intereses."
  },
  {
    title: "Antecedentes legales de la propiedad",
    content:
      "Conjunto de antecedentes y documentos requeridos para estudiar la historia de dominio de la propiedad, a fin de perfeccionar la transferencia de la misma..."
  },
  // 👉 Agrega aquí el resto de los ítems que tienes
];
---

<div class="w-full max-w-3xl mx-auto p-4">
  {items.map((item, index) => (
    <div 
      class="border-b border-gray-300"
      x-data={`{ open: ${index === 0 ? "true" : "false"} }`}
      x-cloak
    >
      <!-- Header -->
      <button 
        class="w-full flex justify-between items-center py-4 text-left text-lg font-semibold text-gray-800 hover:text-blue-600"
        onclick={`this.parentElement.__x.$data.open = !this.parentElement.__x.$data.open`}
      >
        <span>{item.title}</span>
        <svg 
          xmlns="http://www.w3.org/2000/svg" 
          class="h-5 w-5 transform transition-transform duration-300" 
          :class="open ? 'rotate-180' : ''" 
          fill="none" 
          viewBox="0 0 24 24" 
          stroke="currentColor"
        >
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
        </svg>
      </button>

      <!-- Content -->
      <div 
        class="overflow-hidden transition-all duration-300 max-h-0 text-gray-600 text-base leading-relaxed"
        :class="open ? 'max-h-96 py-2' :"
      >
        {item.content}
      </div>
    </div>
  ))}
</div>

<style is:global>
  [x-cloak] { display: none !important; }
</style>
